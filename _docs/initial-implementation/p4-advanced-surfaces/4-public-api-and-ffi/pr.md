expose pip, anchoring, and capture exclusion in public and FFI APIs

Adds the Pip struct with DWM thumbnail methods, anchor_to/unanchor/set_capture_excluded on all surface types, and 6 new FFI functions with C-compatible config/event types. The .def file now lists 41 exports.
